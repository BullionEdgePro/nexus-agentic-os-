{
  "name": "nexus-agentic-os",
  "private": true,
  "version": "0.1.0",
  "description": "Multi-tenant WhatsApp Business agentic OS serving Zipicka, Juris Prime, Juris Prime Legal, SFS International, and Atif Ali Production.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "dev:worker": "npm run dev:worker -w apps/api",
    "db:migrate": "npm run migrate -w packages/db",
    "db:seed": "npm run seed -w packages/db",
    "build": "npm run typecheck --workspaces --if-present && npm run build -w apps/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test -w apps/api && npm run test -w apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
